services:
  - type: web
    name: scisynth-server
    runtime: python
    buildCommand: pip install -r requirements.txt && python -m py_compile main.py app.py
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
